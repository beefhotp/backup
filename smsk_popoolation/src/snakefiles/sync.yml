# Environment for sync subworkflow
name: sync
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - perl
    - pigz
    - openjdk
prefix: /exports/cmvm/eddie/eb/groups/HighlanderLab/anaconda/envs/sync
